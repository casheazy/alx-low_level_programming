#include <stdio.h>

/**
 *main - prints letters of alphabet in lower and upper case
 *
 *Return: returns value of 0
 */
int main(void)
{
char a;
char A = 'A';
for (a = 'a'; a <= 'z'; ++a)
putchar(a);
while ( A <= 'Z')
putchar(A);
++A;
putchar('\n');
return (0);
}
