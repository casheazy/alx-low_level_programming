#include <stdio.h>
/**
 * main-prints a string using printf
 *
 * Return: returns value of 0 if successful
 */
int main(void)
{
printf("with proper grammar, but the  outconme is a piece of art");
return (0);
}
